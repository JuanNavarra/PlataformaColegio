@{
}

<div class="card">
    <div class="card-header">
        <h3 class="card-title">Listado de perfiles con autorización</h3>
    </div>
    <div class="row">
        <div class="col-md-1 col-sm-2 col-12">
            <div class="info-box">
                <span class="info-box-icon bg-primary"><i class="fas fa-plus"></i></span>
                <!-- /.info-box-content -->
            </div>
            <!-- /.info-box -->
        </div>
    </div>
    @await Html.PartialAsync("_TablaPerfiles")
</div>